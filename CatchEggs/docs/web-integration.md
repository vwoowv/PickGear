# CatchEggs 웹 연동

PickGear와 통합된 최신 규약은 [web-control.md](web-control.md)를 참고하세요. 이전 `RETURN_TO_TITLE`, `GAME_EXIT`, `GAME_STATE` 형식은 공통 규약의 `GO_TO_TITLE`, `GAME_EXITED`, `GAME_COMMAND_RESULT`로 교체되었습니다.
